import React from 'react';
import ReactDOM from 'react-dom/client';
import Counterapp from './counter';

const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(<Counterapp></Counterapp>)
